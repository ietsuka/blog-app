<body>
  <article>
    <div class="container" id="withdraw">
      <section class="main_area">
        <div class="center_content">
          <h2>本当に退会しますか？</h2>
          <p>退会する場合は、『退会する』をクリックしてください。</p>
          <div class="btn_area">
            <%= link_to '退会しない', root_path, class: "btn" %>
            <%= link_to "退会する", delete_withdraw_path(@user.id), method: :delete, class: "btn" %>
          </div>
        </div>
      </section>
    </div>
  </article>
</body>